version: '3.8'

services:
  web:
    build: .
    container_name: tecnovilladiego-app
    ports:
      - "8080:80"
    volumes:
      - .:/usr/share/nginx/html
    environment:
      - NGINX_HOST=localhost
      - NGINX_PORT=80
